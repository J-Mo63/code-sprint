<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sudo_code.codesprint">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Hacker">
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.HomeActivity"
            android:label="@string/title_activity_home" />
        <activity
            android:name=".activity.BeginChallengeActivity"
            android:label="@string/title_activity_begin_challenge" />
        <activity
            android:name=".activity.UserChallengeActivity"
            android:label="@string/title_activity_view_user_challenge" />
        <activity
            android:name=".activity.ChallengeActivity"
            android:label="@string/title_activity_challenge" />
        <activity
            android:name=".activity.ResultsActivity"
            android:label="@string/title_activity_results" />
        <activity
            android:name=".activity.FollowingActivity"
            android:label="@string/title_activity_following" />
    </application>

</manifest>